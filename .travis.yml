language: java
sudo: false
install: true
jdk: openjdk10

script:
  - mvn verify -B -e -V

after_success:
  - mvn org.jacoco:jacoco-maven-plugin:prepare-agent coveralls:report

cache:
  directories:
    - '$HOME/.m2/repository'

notifications:
  email: false
